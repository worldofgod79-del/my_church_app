name: my_church_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  go_router: ^14.0.0          # Updated
  scrollable_positioned_list: ^0.3.8
  http: ^1.2.0
  shared_preferences: ^2.3.0  # Updated
  share_plus: ^10.1.0         # Updated to latest (Fixes Web Error)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/bible_json/